<div class="container mx-auto mt-8">
  <%= render partial: 'shared/components/h1', locals: { title: 'My Tanks' } %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <%= render partial: 'tanks/tank', collection: @tanks, locals: { show_button: true } %>
    <%= link_to 'Add New Tank', new_tank_path, class: 'flex justify-center items-center max-w-sm bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out', data: { turbo_frame: '_top' } %>
  </div>
</div>
